// function romanToInt converts from integer numbers to roman
// code by: Muhammad Saeed

// variable of the result
let totalRomanNum = 0;

// the roman chars map to ints
const romans = {
    1: "I",
    5: "V",
    10: "X",
    50: "L",
    100: "C",
    500: "D",
    1000: "M"
};

function intToRomanEval(intNum) {
    let romanNum = romans[intNum];
    return romanNum;
}

let intToRoman = function(totalIntNum) {
    switch (totalIntNum.length) {
        case 1:

    }
};

romanToInt(10000);